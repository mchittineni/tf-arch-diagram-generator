# Changelog

Release notes are generated by [semantic-release](https://semantic-release.gitbook.io/)
from [Conventional Commits](https://www.conventionalcommits.org/) and published on the
[GitHub Releases](https://github.com/mchittineni/tf-arch-diagram-generator/releases) page.

## [Unreleased]

### Added

- **Reference-based architectural link extraction** — Scans Terraform configuration
  expressions (`configuration.root_module.resources[].expressions.*.references`) and
  performs depth-bounded attribute tree inspection (`after` and `before`) to link
  real dependencies such as KMS keys, security groups, IAM roles, secrets, target
  groups, route tables, and peering connections.
- **Perimeter-snapped connector routing** — Accurate ray-box perimeter intersection
  (`computePerimeterAnchor`) calculates connection endpoints at outer card boundaries
  with clearance offsets, ensuring arrowheads and flow lines sit visibly on card borders
  rather than being hidden beneath opaque node cards.
- **Relationship styles and markers** — Multi-style visual differentiation for:
  - **Traffic flows**: Solid blue/violet natural cubic Bezier flows with active flow animations.
  - **Security & Encryption**: Dashed amber curves with dedicated security markers (`#arrowhead-security`).
  - **Peering & Transit**: Distinctive violet peering connectors.
  - **Dependencies**: Dashed indigo flows.
- **Dual-directional connection spotlighting** — Hovering or selecting any resource
  highlights inbound paths in blue (`.edge-inbound`) and outbound paths in violet
  (`.edge-outbound`), while fading back unrelated architecture.
- **Interactive edge controls & tooltips** — Added a toolbar button to toggle connection
  label pills on/off, interactive hover tooltips with relation metadata, and click-to-select
  on connector paths.
- **Connected Links inspector** — Added an interactive **Connected Links** card to the
  Resource Inspector drawer, categorizing inbound ($\leftarrow$) and outbound ($\rightarrow$)
  links with relation badges, icons, and one-click canvas navigation.
- **Expanded cloud resource coverage** — Added WAFv2, ACM, Transit Gateway, Route Tables,
  Step Functions, Kinesis, and AppSync for AWS; Cloud Armor, Artifact Registry, CMEK KMS,
  Serverless VPC Access, Cloud Tasks, and Workflows for GCP; and Private Endpoints, ACR,
  Key Vault, NSG rules, and VNet peering for Azure (total mapped resources now 280+).
- **Expanded test suite** — Added test cases for reference extraction, perimeter snapping
  calculations, edge toggle controls, directional spotlighting, and inspector link navigation
  (57 tests passing with $\ge 95\%$ project-wide line coverage).
- **Python 3.12, 3.13, and 3.14 support** — Officially tested and certified the Python
  distribution (`tf_arch` package and `tf-arch` CLI wrapper) across Python 3.12, 3.13,
  and 3.14 with dedicated CI matrix jobs.

### Changed

- **Python baseline** — Raised `requires-python` in `python/pyproject.toml` to `>=3.12`
  with classifiers and documentation updated for Python 3.12, 3.13, and 3.14.
- **CI workflow enhancements** — Updated `.github/workflows/ci.yml` matrix to validate
  Python 3.12, 3.13, and 3.14, switched diagram artifact uploads and wheel builds to Node.js 24,
  and added directory creation guards (`mkdir -p out`) for diagram renders.
- **Dependabot grouping** — Configured automated grouped PRs for `npm` dependencies (monthly)
  and `github-actions` (semiannually) in `.github/dependabot.yml` to minimize notification noise.

## 1.2.1

Maintenance release resolving dependency conflicts in the release toolchain.

### Fixed

- **Release automation dependencies** — Pinned `conventional-changelog-conventionalcommits`
  to a compatible 9.x version to prevent parsing errors during semantic-release publishing.

## 1.2.0

Added a first-class Python distribution, Homebrew tap release automation, and major CLI
and parser robustness hardening.

### Added

- **Python distribution & PyPI package** — Installable via `pip install tf-arch-diagram-generator`
  with zero third-party dependencies, bundling the exact engine and driving it via local Node.js.
  Provides the `tf-arch` console command and an importable `tf_arch` API (`render`, `inspect`,
  `find_node`). Includes a dedicated Python unit test suite.
- **Homebrew tap formula generation** — Added automated formula generator script
  (`scripts/brew-formula.mjs`) and GitHub Actions workflow publishing to Homebrew tap repositories.
- **Piped standard input** — The CLI accepts `-` as the plan argument to read Terraform plans
  directly from stdin (`terraform show -json tfplan | tf-arch render -`).
- **Configuration-based hierarchy resolution** — The parser now inspects `configuration.root_module`
  references to associate subnets and VPCs when resource IDs are not yet known during create plans.
- **CLI input validation** — The CLI strictly validates flag arguments, rejecting missing values,
  unrecognized options, and incompatible flags with contextual exit codes and usage hints.

### Changed

- **Inferred edge capping** — Parser caps inferred relationships to prevent layout degradation
  on extremely large plans, warning the user via stderr while keeping `--json` clean.
- **Resource address index normalization** — Strips index brackets from addresses (e.g. `module.vpc.aws_subnet.public[0]`)
  using a fast linear scan rather than backtracking expressions.
- **Single file read** — CLI reads plan files into memory in a single atomic read operation,
  avoiding race conditions from check-then-read patterns.
- **Atomic diagram writes** — `tf-arch render` writes the destination SVG file first and
  classifies write errors afterwards, preventing misleading missing file errors.
- **Accessible import modal** — Enhanced modal with ARIA accessibility roles and inline error
  messaging that preserves existing diagram state on invalid plan input.

## 1.1.0

Official cloud provider architecture icons and interactive diagram canvas interactions.

### Added

- **Official cloud provider icon sets** — Replaced generic provider icons with official
  AWS, Google Cloud, and Azure architecture icons embedded directly as optimized SVG symbols.
- **Icon build and validation pipeline** — Added `scripts/build-icon-modules.mjs` and
  `npm run icons:build:check` in CI to ensure embedded icon definitions stay synchronized with
  official vendor asset bundles.
- **Connection spotlighting** — Hovering or selecting a node dynamically spotlights immediate
  connections while fading unrelated diagram elements.
- **Canvas keyboard controls** — Added keyboard navigation shortcuts: pan with arrow keys or
  WASD, zoom with `+` / `-`, reset zoom with `0`, and fit diagram to viewport with `f`.
- **Double-click zoom-to-node** — Double-clicking any resource smoothly animates and centers
  the viewport on that card.
- **Vendor asset attribution** — Added `assets/icons/NOTICE.md` documenting official vendor
  terms of use for AWS, Google Cloud, and Azure architecture icon sets.

## 1.0.1

Package metadata and trusted publishing configuration.

### Fixed

- **npm package metadata** — Completed missing repository, bug tracker, homepage, and
  funding fields in `package.json`.
- **OIDC trusted publishing** — Configured GitHub Actions publishing with npm Provenance
  attestations via OpenID Connect (OIDC).

## 1.0.0

First open-source release. The tool went from an AWS-only web app to a
multi-cloud, publishable npm package.

### Added

- **Google Cloud support** — 82 `google_*` resource types mapped across 37
  service icons, with `google_compute_network` / `google_compute_subnetwork`
  hierarchy, nested `network_interface[].subnetwork` resolution, zone/region
  grouping, label support, and Cloud DNS → Global LB → Cloud Run / MIG →
  Cloud SQL / Spanner / Firestore / Pub/Sub edge inference.
- **Azure support** — 103 `azurerm_*` / `azuread_*` resource types mapped across
  40 service icons, with `azurerm_virtual_network` / `azurerm_subnet` hierarchy,
  nested `ip_configuration[].subnet_id` and `default_node_pool[].vnet_subnet_id`
  resolution, location and resource-group metadata, and DNS → Front Door →
  Application Gateway → VMSS / AKS → Azure SQL / Cosmos DB / Service Bus edge
  inference.
- **Multi-cloud plans** — a plan spanning several providers renders one labelled
  band per cloud; inferred edges never cross provider boundaries. New sidebar
  filter and navbar badges for the clouds detected in a plan.
- **npm package** — installable and runnable via `npx`, with a `tf-arch` binary
  and a DOM-free programmatic API (`parseTerraformPlan`,
  `computeArchitectureLayout`, `renderStandaloneSvg`, `planToSvg`, provider
  registry helpers).
- **CLI** — `tf-arch serve` (interactive viewer, optionally preloaded with your
  plan), `tf-arch render` (headless SVG, no browser required) and
  `tf-arch inspect` (human and `--json` plan summaries).
- **Provider plug-in architecture** — each cloud is a directory under
  `src/providers/` declaring its type prefixes, icon set, resource map,
  hierarchy attribute keys, vocabulary and edge inference. Adding a cloud
  requires one registry entry.
- **Five new example architectures** — GCP web platform, GCP serverless data
  platform, Azure 3-tier, Azure AKS platform and a multi-cloud landing zone,
  available in the viewer and as standalone files in `examples/`.
- **Open-source project scaffolding** — MIT license, contributing guide,
  code of conduct, security policy, issue/PR templates, Dependabot config, and
  GitHub Actions workflows for CI (Node 22/24/26), semantic-release publishing
  with provenance, Conventional-Commit linting, a security pipeline (npm audit,
  gitleaks over full history, CodeQL security-extended, vendor-SVG scan) and a
  GitHub Pages deploy of the live demo.
- **Reference icon pipeline** — the official AWS/GCP/Azure architecture icon
  sets (1,145 SVGs) tracked under `assets/icons/` with a checksummed manifest,
  refreshed semiannually by a scheduled workflow that opens a PR only on real
  upstream drift; every refresh is gated by an active-content scan. The icons
  stay under their vendors' terms and are excluded from the npm package.
- **Hardened local server** — `tf-arch serve` validates the Host header
  (blocks DNS rebinding), sends a strict CSP plus security headers, accepts
  only GET/HEAD, resolves symlinks before its containment check, serves
  fingerprinted assets with immutable caching + ETag/304 revalidation, and
  shuts down gracefully on SIGINT/SIGTERM. The CLI refuses to run on
  end-of-life Node versions (supported: the active lines, currently 22/24/26).
- **Test suite and coverage gate** — 53 tests on Node's built-in runner:
  parsing and all three provider hierarchies, multi-cloud isolation, the full
  app mounted in jsdom, canvas/modal/inspector interactions, hostile-plan
  injection, the published API contract, and socket-level server tests.
  `scripts/check-coverage.mjs` enforces ≥90% line coverage per file and
  overall (currently ~99%), and fails if any source file has no test at all.

### Changed

- Parser output is now provider-agnostic: `parentVpcId` → `parentNetworkId`,
  `vpcNodes` → `networkNodes`, plus `providerId`, `region`, `group`, `tags` and
  `module` on every node. Plans are also read from `planned_values` /
  `prior_state`, including child modules.
- SVG generation moved into a shared pure-string renderer, so the interactive
  canvas and the CLI produce identical output from the same code.
- The inspector, sidebar and navbar use each provider's own vocabulary rather
  than AWS terms; the UI palette is provider-neutral with per-cloud accents.
- Diagram export now produces a titled, legended standalone SVG document.

### Fixed

- Resource names, types, addresses, tags and attribute keys are escaped before
  being written into SVG and HTML, so a plan containing markup can no longer
  break or inject into the rendered diagram.
- The app no longer renders a blank page: initialising the diagram canvas used
  to destroy the zoom controls it shared a container with, crashing startup.
  The canvas now owns a dedicated mount node.
- Embedded service icons carry an explicit viewport size. A nested `<svg>`
  without width/height defaults to the full canvas, which blew each icon up to
  cover the entire diagram.
- Canvas bounds track the real drawing extent instead of a fixed 1200×800
  minimum, so fit-to-screen and exported SVGs no longer carry dead space.
- The local server survives unreadable files instead of crashing on an
  unhandled stream error, and reports a port conflict without a stack trace.
- Resources whose subnet or network lives outside the plan are laid out as
  standalone nodes instead of disappearing from the diagram.
- `terraform show -json` output with no `resource_changes` block no longer
  renders an empty canvas.
